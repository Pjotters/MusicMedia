// Basis JS voor MusicMedia
// Hier komt dynamische content, ophalen van muziek, etc.
document.addEventListener('DOMContentLoaded', () => {
    // Placeholder voor dynamisch laden van muziek
    const musicList = document.getElementById('music-list');
    musicList.innerHTML = '<em>Muzieklijst wordt geladen...</em>';
});
